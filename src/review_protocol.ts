export const FEATURE_CONTRACT_REVIEW_ACTION = "Feature Contract Review";
export const STEP_EVIDENCE_REVIEW_ACTION = "Step Evidence Review";

export function renderNextRequiredAction(action: string): string {
  return `Next required action: ${action}`;
}

{
  "name": "harness-mcp",
  "version": "0.0.1",
  "description": "Business-discovery driven MCP server for BDD contracts, verification, and AI code-quality gates.",
  "license": "MIT",
  "module": "src/index.ts",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jiongQAQ/harness-mcp.git"
  },
  "homepage": "https://github.com/jiongQAQ/harness-mcp#readme",
  "bugs": {
    "url": "https://github.com/jiongQAQ/harness-mcp/issues"
  },
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "harness-mcp": "dist/index.mjs"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun run --watch src/index.ts",
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --format=esm --outfile=dist/index.mjs --banner:js='#!/usr/bin/env node\nimport { createRequire as __harnessCreateRequire } from \"node:module\";\nconst require = __harnessCreateRequire(import.meta.url);' --external:effect --external:sury --external:@valibot/to-json-schema",
    "prepack": "bun run build",
    "typecheck": "tsc --noEmit",
    "pack:check": "bun run build && npm pack --dry-run"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "esbuild": "^0.28.0",
    "typescript": "^5"
  },
  "dependencies": {
    "@cucumber/gherkin": "^39.0.0",
    "@cucumber/messages": "^32.3.1",
    "fast-glob": "^3.3.3",
    "fastmcp": "^4.0.1",
    "minimatch": "^10.2.5",
    "yaml": "^2.8.3",
    "zod": "^4.3.6"
  }
}
